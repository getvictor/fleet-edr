# Tasks

- [x] `Subcommand`, `CommandArguments` and `EnvAssignments` computed from argv
- [x] Registered in the taxonomy, so the load-time field check covers them
- [x] Table tests pinning each field's exact contents
- [x] Differential property tests asserting each field reproduces the Go matcher it will replace
- [ ] Convert the three rules onto these fields and delete the Go matchers (#761)

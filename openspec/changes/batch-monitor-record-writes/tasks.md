# Tasks

- [x] Collect a batch's monitor records in the engine and write them when evaluation ends, on every exit.
- [x] Add `InsertMonitorRecords`: one archive read, chunked transactions, shared link and evidence writers with the alert insert.
- [x] Engine and store tests, and mutation checks.
- [x] Re-run the 200-finding burst on the dev server and record both timings.

# Tasks

- [ ] Add a route-driven sub-navigation component and use it on both the catalogue and the coverage view.
- [ ] Fold the Coverage entry into Rules, keeping the Rules entry active on either path.
- [ ] Keep `/coverage` routable so existing links and bookmarks resolve.
- [ ] Tests for the tab rendering, the active tab on each path, and the top entry staying active across both.

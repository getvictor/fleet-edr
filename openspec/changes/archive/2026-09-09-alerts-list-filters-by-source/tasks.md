# Tasks

- [x] 1.1 Verify the `source` filter is actually served before specifying it: the handler reads the query parameter and the store applies it to both the list and count queries.
- [x] 1.2 Add `source` to the filter list and give it a scenario.
- [x] 1.3 State the conjunctive-combination rule the existing text only implied.
- [x] 1.4 Keep both existing scenarios verbatim so their markers stay attached.

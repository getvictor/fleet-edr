# Tasks

- [x] `rule_content.read` / `rule_content.write` in the UI permission set; Rules entry in the top navigation
- [x] Rules catalogue page: name, identifier, severity, mode in force, shipped or yours; search and ownership filter
- [x] Rule document panel on the rule page, gated on `rule_content.read`, with a built-in rule's lack of a stored document stated
- [x] Text fetch sharing the JSON fetch's session, reauth, and authorization handling
- [x] Manual QA in Chrome against the dev server

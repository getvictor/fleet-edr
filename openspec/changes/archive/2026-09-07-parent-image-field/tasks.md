# Tasks

- [x] `ParentImage` in the exec taxonomy, supplied by the caller rather than read from the payload
- [x] A catalog helper resolving it through the process graph, preserving the decline-on-unresolved behaviour
- [x] `shell_from_office` converted onto it, reaching `portable: standard`
- [x] Guard for the shared list the conversion had to inline
- [ ] The 11 corpus rules evaluate, once #764 imports them

# Tasks

## 1. Rename what operators read

- [x] 1.1 Change the canonical name to describe the launch, lookup and connect chain.
- [x] 1.2 Record in the rule's doc comment that the identifier deliberately does not match, and why changing it would cost more than it buys.
- [x] 1.3 Regenerate the rule pack and `docs/detection-rules.md`.
- [x] 1.4 Update the demo trigger script, demo README, seed corpus and project README, which surface the title to a viewer.
- [x] 1.5 Leave released changelog entries alone: they record what the rule was called when those versions shipped.

## 2. Verification

- [x] 2.1 A test pins that the identifier and the canonical name are both what they should be, so neither drifts back.
- [x] 2.2 The name carries no periodicity claim.

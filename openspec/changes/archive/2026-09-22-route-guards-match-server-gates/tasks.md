# Tasks

- [ ] Gate the Coverage nav entry and route on `alert.read`.
- [ ] Regate the rule catalogue, a rule's detail and its monitor records on `alert.read`.
- [ ] Show the built-in rules panel only to an operator holding `rule_content.read`, so the catalogue does not carry a failing panel for one who lacks it.
- [ ] Give the landing redirect a destination that renders the no-access state when no entry matches.
- [ ] Tests for each regated route, and for the panel.

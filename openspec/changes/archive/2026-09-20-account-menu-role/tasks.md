# Tasks

- [x] Return the session's global role ids from `GET /api/session`, and document the field.
- [x] Name the roles and the sign-in method in the account menu dropdown, from one shared set of role labels.
- [x] Server and UI tests; Chrome QA against a dev server.

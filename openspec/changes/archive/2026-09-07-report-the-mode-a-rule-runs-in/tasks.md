# Tasks

- [x] Resolve a rule's global mode and its source in one call, through a read surface separate from the engine's.
- [x] Report `mode` and `mode_source` on `GET /api/rules`, and document both in the OpenAPI spec.
- [x] Show the mode in force on the rule detail view, and say whether a setting or the rule's declaration produced it.
- [x] Offer monitor in the admin table's mode control, and prompt for a reason on the changes that reduce what a rule does.
- [x] Delete the engine's duplicate rule-metadata builder, which production does not use.

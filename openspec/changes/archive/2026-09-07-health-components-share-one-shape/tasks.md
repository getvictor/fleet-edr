# Tasks

- [x] Split the component row into an explicit head row (badge, name) and detail row (message, age)
- [x] Drop the wrapping flex line that made the shape depend on the name's length
- [x] Playwright regression covering agreement across components, since the defect was one component differing from its siblings
- [x] Verified against the lane-B dev server: four components, no message sharing its name's baseline, one distinct message left edge

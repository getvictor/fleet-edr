# Tasks

- [x] Patterns compile to star-separated segments at rule load
- [x] The first segment anchors at the start and the last at the end
- [x] Middle segments are found left to right without backtracking
- [x] Equivalence property against the frozen backtracking implementation
- [x] Benchmarks pairing each shape with the reference on the same input
- [ ] Bound the `?`-segment residual when patterns become operator-authored (#767)

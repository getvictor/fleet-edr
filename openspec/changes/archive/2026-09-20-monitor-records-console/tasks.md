# Tasks

- [x] `listAlerts` takes `disposition` and `rule_id`; `Alert` carries `disposition`
- [x] Monitor records page, reached from a records link beside the Observed count
- [x] The page and the Observed note explain why records can be fewer than matches
- [x] A monitor record's detail shows a badge instead of triage, and links back to its rule's records
- [x] Manual QA in Chrome against the dev server

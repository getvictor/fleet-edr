# Tasks

- [x] Add `reboot_required` to the agent's health reasons and mark the network extension with it when the receiver logs its reboot-required signal.
- [x] Unit tests for the registry state, the loop's once-per-episode hook, and the agent wiring; mutation-check them.
- [x] Verify on a VM with a staged network extension upgrade that the console shows the reason.

# Tasks

- [x] Extension: watched set as built-in paths plus a pushed set, decoded, persisted, and applied as a mute difference on the running client
- [x] Extension: `watched_paths.update` XPC message
- [x] Agent: `set_watched_paths` command forwarded over XPC
- [x] Live exercise on edr-dev: a pushed path is observed without a restart, a dropped one stops, sudoers stays watched, and the set survives an extension restart

# Tasks

- [x] `persistence_launchagent` converted, matching binary, subcommand and target
- [x] `dyld_insert` converted, matching the leading assignment window
- [x] Go matchers, params and algorithms removed for both
- [x] Both pre-conversion matchers frozen as equivalence oracles, over the complete predicate
- [ ] `shell_from_office` and `sudoers_tamper`, once #771 and #772 land

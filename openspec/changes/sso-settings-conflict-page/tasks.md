# Tasks

- [x] Send the version the page was shown, and the one its last save returned.
- [x] Report a conflict as what happened rather than as a status code, and leave other failures reading as themselves.
- [x] Route the save through the shared typed-mutation client so the wire code survives.

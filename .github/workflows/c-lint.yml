name: C lint

on:
  push:
    branches: [main]
    paths:
      - "agent/**/*.c"
      - "agent/**/*.h"
      - ".clang-tidy"
      - ".github/workflows/c-lint.yml"
  pull_request:
    paths:
      - "agent/**/*.c"
      - "agent/**/*.h"
      - ".clang-tidy"
      - ".github/workflows/c-lint.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  clang-tidy:
    name: clang-tidy
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Install LLVM (clang-tidy)
        run: brew install llvm

      - name: Run clang-tidy
        run: find agent/xpcbridge \( -name '*.c' -o -name '*.h' \) -exec "$(brew --prefix llvm)/bin/clang-tidy" {} +

      - name: Check formatting
        run: find agent \( -name '*.c' -o -name '*.h' \) -exec "$(brew --prefix llvm)/bin/clang-format" --dry-run --Werror {} +

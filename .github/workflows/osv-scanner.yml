name: OSV-Scanner

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    # Weekly rescan catches new CVEs against unchanged dependencies.
    - cron: "23 4 * * 1"
  workflow_dispatch:

# SARIF upload requires security-events:write; granted only to the reusable
# workflow invocation below so the default token (for checkouts etc.) stays
# minimal. The top-level `permissions: {}` grants nothing (does not trigger
# Sonar githubactions:S8264, which forbids granting read scopes at workflow
# level) while still satisfying Scorecard Token-Permissions, which wants
# top-level permissions explicitly declared.
permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  scan:
    name: osv-scanner
    uses: google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@9a498708959aeaef5ef730655706c5a1df1edbc2 # v2.3.8
    permissions:
      actions: read # reusable workflow reads workflow context for SARIF categorization
      contents: read # required to check out the repo for scanning
      security-events: write # required to upload SARIF to GitHub code scanning
    with:
      # Recursive scan of every manifest in the repo: go.mod (agent + server),
      # ui/package-lock.json, and .github/workflows pins.
      scan-args: |-
        --recursive
        ./
      # Block PRs on any new advisory across the dep tree. govulncheck (in
      # go-vulncheck.yml) is the reachability-aware gate; OSV-Scanner is the
      # wider net catching unreachable but still-known-bad deps. The initial
      # 21-vuln backlog was cleared by bumping golang.org/x/{crypto,net,sys},
      # so we can now gate without a perma-red baseline.
      fail-on-vuln: true

package seed

// This package generates no random password; the bootstrap-token redemption flow in server/identity/internal/breakglass owns the
// credential. The package's behavior is covered by the integration tests in admin_test.go.

// Package service is the rules orchestrator: a single struct that wraps the rule catalog and exposes the api.Lister and
// api.RuleProvider surfaces consumed by the operator handler and the detection engine.
package service

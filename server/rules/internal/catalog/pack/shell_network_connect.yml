# Generated by tools/gen-rule-pack from the registered rule catalog.
# Everything here is regenerated from the rule's Go documentation EXCEPT the detection block and x-engine.params. Those are
# the rule's own logic and the values it reads, so they are authored by hand and regeneration re-emits them verbatim,
# comments included.
# Run `task docs:rule-pack` after changing a rule's documentation or metadata.
title: Shell outbound connection
id: 3a545b99-f72d-54b5-888d-40af23ad811f
status: stable
description: |-
  Flags a non-shell process that spawns a shell which, within 30 seconds, makes an outbound network connection.

  Detects the chain shape: non-shell parent → shell child → outbound network_connect.

  The rule fires on the LAST link (the connection) rather than the shell's exec. That makes it race-immune across the agent's flush boundaries: a chain completing in ~150ms but straddling a 1-second flush boundary still resolves, because the whole ancestor chain has been ingested by the time the trigger lands.

  Split from `suspicious_exec` (issue #776), which fired on this shape or a temp-directory exec. A chain doing both now raises one alert per rule.

  30 seconds is the temporal cap between the shell exec and the connection.
author: Fleet EDR
level: high
tags:
  - attack.t1059
  - attack.t1105
logsource:
  product: macos
  category: network_connection
falsepositives:
  - Interactive SSH where an admin curls a tool. Add a parent-path-glob exclusion for `/usr/libexec/sshd-session` via the detection-config surface if that is a routine workflow on the host class.
  - Developer tooling that shells out and connects (Claude Code, lefthook git hooks, git, IDEs). These install under version-stamped paths, so add a parent-path-glob exclusion such as `*/claude/versions/*` that survives upgrades.
x-engine:
  rule_id: shell_network_connect
  type: graph
  portable: none
  portability_note: The rule's logic is a Go implementation named by x-engine.algorithm, not a declarative detection block, so there is nothing here for another engine to evaluate. Rules whose logic can be expressed in Sigma are being converted separately; until a rule's logic lives in its file, this stays the honest answer.
  event_types:
    - network_connect
  algorithm: ancestor_walk_outbound_connect
  params:
    # Maximum time between the shell exec and the outbound connection. Real chains complete in seconds, so 30s is a generous
    # ceiling rather than a tuned threshold. Deliberately a separate value from suspicious_exec's identical one: independent
    # tuning is a reason issue #776 split the rules, and sharing the number would have quietly re-coupled them.
    window: 30s
  exclusions:
    - parent_path_glob
    - team_id
    - signing_id
    - cdhash
  limitations:
    - The window bounds how long after the shell exec a connection still counts; long-tail post-shell activity is missed by design. Set in x-engine.params.window.
    - An outbound DNS lookup (port 53) to a local-resolver-class address (loopback, RFC1918, link-local, CGNAT 100.64.0.0/10, IPv6 ULA/link-local) is treated as name resolution and does not fire; a lookup to a publicly routable resolver still does.
    - 'Exclusions saved against `suspicious_exec` before the split (issue #776) do not apply here, because exclusions are keyed by rule id. Re-add any that should silence this shape too.'

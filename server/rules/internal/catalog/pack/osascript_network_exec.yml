# Generated by tools/gen-rule-pack from the registered rule catalog.
# Everything here is regenerated from the rule's Go documentation EXCEPT the detection block and x-engine.params. Those are
# the rule's own logic and the values it reads, so they are authored by hand and regeneration re-emits them verbatim,
# comments included.
# Run `task docs:rule-pack` after changing a rule's documentation or metadata.
title: AppleScript dropper
id: ebc81a2c-1d27-5a2b-9d97-700ae00cb9dc
status: stable
description: |-
  Critical-severity catch on the canonical macOS commodity-dropper chain: osascript fetches a stage-2 over the network and runs it from /tmp.

  Fires on the LAST link of the chain: an exec from a temp directory whose process tree has both an osascript ancestor and a curl/wget sibling within the osascript's 30-second descendant window. This shape is the recognisable signature of macOS commodity malware staged via AppleScript.

  Reverse-direction triggering is deliberate: by the time the temp-exec event lands, the entire ancestor chain has already been ingested and materialised by earlier batches, so the rule is race-immune. Forward triggering (fire on the osascript exec, look for descendants) misses chains that complete across an agent flush boundary.

  The rule requires both halves of the chain to be present, so download-only or temp-exec-only flows do not fire here: those overlap with suspicious_exec.
author: Fleet EDR
level: critical
tags:
  - attack.t1059.002
  - attack.t1105
logsource:
  product: macos
  category: process_creation
falsepositives:
  - 'Internal automation that bootstraps tooling by scripting `curl … | sh` from osascript: extremely rare in managed fleets.'
x-engine:
  rule_id: osascript_network_exec
  type: graph
  portable: none
  portability_note: The rule's logic is a Go implementation named by x-engine.algorithm, not a declarative detection block, so there is nothing here for another engine to evaluate. Rules whose logic can be expressed in Sigma are being converted separately; until a rule's logic lives in its file, this stays the honest answer.
  event_types:
    - exec
  algorithm: descendant_within_window
  params:
    # Rule identity, read-only: what counts as osascript.
    osascript_paths:
      - /usr/bin/osascript
    # The downloaders whose invocation under osascript is the dropper shape. The tunable one: a fleet with its own fetch tool
    # adds it here.
    download_binaries:
      - /usr/bin/curl
      - /usr/bin/wget
      - /opt/homebrew/bin/curl
      - /opt/homebrew/bin/wget
    # Bounds the descendant walk forward from the osascript exec. Real droppers stage and run within a couple of seconds; 30s
    # keeps slow networks in bounds without turning the rule into a sliding-window fish-hook.
    window: 30s
  limitations:
    - The descendant window bounds how long after the osascript exec a stage-2 still counts; longer-running chains are missed by design. Set in x-engine.params.window.
    - 'Does not cover Python URL fetches or AppleScript built-in URL access: only flags the explicit curl/wget shape.'

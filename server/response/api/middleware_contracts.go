// Package-level contracts that other contexts' middleware would
// satisfy. response has no agent-facing middleware of its own (the
// host-token middleware lives in endpoint), so this file is empty
// in phase 4. Reserved so the api package layout stays congruent
// with identity / endpoint / rules.

package api

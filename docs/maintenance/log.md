# Schedule audit log

Append one line per scheduled-task run, even when there are no findings. Format:

```
YYYY-MM-DD  <task-name>  <result>  [PR #N | issue #N | no findings]  [notes]
```

`<result>` is one of `done`, `partial`, `skipped`. `partial` means time budget hit before the task was complete; the next run
should pick up where this one left off.

Keep `[notes]` to ONE tight line: the headline outcome (counts, the one or two things changed) plus the PR / issue / branch
reference. Do NOT narrate the run, enumerate verified-clean items, or paste the findings. The full detail belongs in the PR
body; this log is just the audit trail. If it doesn't fit on a line, it's too long.

The log is the audit trail proving the cadence is being honoured. An empty log over a quarter is the signal to drop the schedule
or change the cadence - running tasks with no record is the same as not running them.

---

<!-- entries below, newest at the bottom -->

2026-05-03  doc-accuracy-sweep  partial  no findings  scope: root READMEs + `docs/*.md`; subdirs deferred to next rotation
2026-05-12  best-practices-refresh  done  ADR-gap issue filed  1 demotion, 1 path fix, 4 new unchecked items
2026-05-19  architecture-drift  done  fixed in-PR  boundary heat map clean; cleared stale `PolicyService`/`ActiveCommandPayload`/`ActiveHostsLister` doc refs + a dead `arch-go.yml` allowance; arch-go green
2026-05-19  dead-code-sweep  done  fixed in-PR  staticcheck U1000 clean; deleted dead `PasteManyRuleType` (ts-prune); other 6 ts-prune hits are false positives; 6 UI-pending operator routes not filed
2026-05-28  stale-implementation-references  done  branch `stale-impl-refs-sweep-2026-05-28` @ 0bf831e  rewrote ~50 stale `Phase N` refs + migrated `claude/`->`ai/` refs; in-test/runtime phase stages left as-is
2026-05-28  doc-accuracy-sweep  done  branch `stale-impl-refs-sweep-2026-05-28` (atop #293)  fixed auth-refactor doc rot (OIDC/break-glass), `EDR_*` env drift, dead paths (`agent/wire`, `server/admin`); aspirational `[ ]` items left
2026-06-09  doc-accuracy-sweep  done  branches `doc-accuracy-{broken,renamed}-2026-06-09` + issue #340  fixed `testing-strategy.md` baseline path + ADR-0007 `XPCServer`->`shared/XPCEventServer` rename; rewrote `architecture.md` pre-ADR-0004 layout
2026-06-18  memory-and-claudemd-audit  done  branch `claude-md-audit-2026-06-18`  CLAUDE.md: `uat:e2e`->`test:e2e`, dev server is HTTPS on `0.0.0.0:8088` since #140. MEMORY.md (uncommitted): rewrote decayed #408 dev-DB entry (now merged to main). All other claims/refs verified clean
2026-06-18  todo-fixme-sweep  done  no findings  only marker is upstream's in vendored `server/apidocs/embed/redoc.standalone.js`; `.build/` hits are untracked. Repo clean
2026-06-18  observability-review  done  no code changes; gap tracked by #348  OTel route coverage complete (shared middleware records route-templated `http.server.request.duration`); prometheus policy clean; 2 EDR dashboards healthy (auth dash env default pinned to `prod-render`, minor); 0 alert rules, telemetry-loss/MTTD alerting already in #348
2026-06-18  adr-audit  done  branch `maintenance-adr-tests-2026-06-18`  12 ADRs re-verified in-force; fixed ADR-0007 stale path (`extension/XPCServer.swift`->`shared/XPCEventServer.swift`; the 06-09 fix never merged); advanced ADR-0012 Proposed->Accepted (capability gating shipped) in file + index; README 0004 ->Implemented to mirror its file. Gap list clean (closed #145 covered mysql + MDM-update)
2026-06-18  test-suite-health  done  branch `maintenance-adr-tests-2026-06-18`  full suite green under `-race`; `-count=3 -race` flake-clean (68 pkgs, 0 races); 12 skips (11 legit env/mode guards), deleted 1 hollow perma-skip (`TestAsyncWriter_DrainGlobalDeadline_SpillsToSlog`); slowest test 3.5s (essential); 10+ semantic spot-checks sound. Deleted-test coverage gap filed as #445 (v0.3.0): drain-deadline spill path needs an injectable deadline to test
2026-06-18  claude-config-audit  done  no committed change (`.claude/` gitignored)  pruned 11 dead allow one-offs from settings.local.json (retired VM `10.0.1.137` + old bearer-token API + Fleet-repo paths); ntfy idle hook valid, no `git push` rule; flagged opsx/openspec skill duplication + broad aws/kubectl breadth for review
2026-06-18  ai-review-bot-config-audit  done  no findings  `.coderabbit.yaml`: all 7 path_instruction globs match the tree, all 5 disabled-tool CI refs (go/ts/swift-lint, actionlint, osv-scanner) exist, no deprecated keys, multi-platform globs intact; pre-merge trip-rate N/A (CodeRabbit manual-only here)
2026-06-19  threat-model-and-security-refresh  done  branch `maintenance-threat-model-2026-06-19`  code-driven boundary inventory; threat-model.md: App Control shipped (agent `set_application_control` cmd + detection-content rows), XPC path `extension/`->`shared/XPCEventServer.swift`, named OTLP egress trust assumption; confirmed agent->server is bearer-over-TLS (not mTLS); 5 medium/low gaps (events-at-rest, CSP, mysql-root, harden-runner, CODEOWNERS) unchanged
2026-06-22  consolidation-pass  done  branch `chore/consolidate-logging-into-internal`  merged 87%/94%-duplicated server+agent `logging.go` into `internal/logging` (scrubbed stale arch-go/codecov/sonar refs); baseline dup 1.3%, cognitive-complexity 5282. Top remaining candidates: `bootstrap.go` cog-26, rbac `"db must not be nil"` x5
2026-06-22  consolidation-pass  done  branch `chore/sonar-maintainability-sweep`  Sonar maintainability batch: rbac `"db must not be nil"` x5 to one sentinel + extracted `refuseIfLastActiveAdmin` (SetUserRole back under the complexity cap, dedups the SetUserStatus guard); metrics `"{event}"` x3 to a const. 3 OPEN criticals cleared. Deferred to own PRs: identity `bootstrap.go` cog-26, `dash-lint` cog-19/17
2026-06-30  architecture-anti-patterns  done  branch `architecture-antipatterns-audit-2026-06-30`  first run, 0 code defects (scans clean); 2 candidates, 1 dropped at self-review. Shipped 1 step-6 catalog delta (Go interface-placement test on the premature-abstraction row) + tightened the step-2 SQL scan to skip HTTP DELETE routes
2026-07-05  stale-implementation-references  done  branch `chore/maintenance-docs-2026-07-05`  stripped ~18 gitignored `ai/` refs (4 ADRs, best-practices, testing-strategy, Taskfile, sonar, 2 tests, 2 READMEs) + fixed stale `phase 1`/`coming soon`/`wave-2`; canonical `server-availability` spec `ai/` ref deferred (needs OpenSpec delta)
2026-07-05  doc-accuracy-sweep  done  branch `chore/maintenance-docs-2026-07-05`  verified 95 non-openspec `.md` (all `task` cmds, `EDR_*` env vars, ports, and doc-to-doc links resolve); fixed 3 renamed paths: `authz.md` `identity/bootstrap/schema.go`->`identity/migrations/00001_initial.sql`, `best-practices.md` `server/retention/`->`server/detection/internal/pipeline/retention.go`, ADR-0008 `ESFSubscriber+FileEvents.swift`->`FileTamperSubscriber.swift`
2026-07-05  todo-fixme-sweep  done  no findings  only marker in tracked first-party/vendored source is upstream's in `server/apidocs/embed/redoc.standalone.js`; `.build/` Swift hits are gitignored. Repo clean (unchanged since 2026-06-18)
2026-07-05  memory-and-claudemd-audit  done  branch `chore/maintenance-docs-2026-07-05`  CLAUDE.md: bounded-context list 5->7 (add `observability`+`visibility` per ADR-0004 amend/ADR-0015), Codecov claim fixed ("mirrors 80%"->non-gating 70%, Sonar 80% authoritative). MEMORY.md (uncommitted): rewrote stale `dev_db_ahead_408` (migrations past 00005), collapsed shipped `control_channel_no_separate_addr` to a pointer (#564), indexed orphaned `signed_host_tokens_qa`
2026-07-05  best-practices-refresh  done  branch chore/maintenance-docs-2026-07-05  17 deltas: 5 demotions (TLS1.2-ciphers->[-] moot, events.json/persist-creds/-lbsm/os.log-lessons->[~]), 4 raised (PR-template+Qodo-autorun->[x], copilot-instructions+coderabbit->[~]); rewrote 5->7 contexts + single-module + dropped alerts-FK/host-token-id; refreshed OCSF 1.6->1.8 + CrowdStrike AIDR; 6 score rows; no new ADR gaps
2026-07-05  adr-audit  done  branch chore/maintenance-docs-2026-07-05  18 ADRs re-verified vs code; advanced 4 Proposed->Accepted (0014 inline-enforcement change archived, 0015 ClickHouse cutover shipped #427/#532, 0017 principal model shipped mig-00005, 0018 Windows Phase 0+1 shipped) in files + index; refreshed 0004/0005 status lines (visibility live, events archive moved) + README; 2 stale "five contexts" refs noted for user (architecture.md:139, find-prior-art SKILL.md:19); gap headline: recommend new ADR for TLS-1.3-only-no-1.2-opt-out + no-in-product-auto-update
2026-07-05  threat-model-and-security-refresh  done  branch chore/maintenance-docs-2026-07-05  code-driven inventory; +2 components (ClickHouse archive comp7, gRPC control-channel gateway comp8) + service-account/API-token authz row; fixed stale TLS-1.2-opt-in (now TLS1.3-only) + events-in-MySQL (dropped, now ClickHouse native TTL) rows; 0 threats demoted
2026-07-05  architecture-anti-patterns  done  no findings  all 4 scans clean: wrong-layer SQL only in demo-seed CLI + leader advisory-lock + identity persistence submodules (established), 0 package-level mutable state (ADR-0010 holds), interfaces unchanged since 06-30; 0 candidates, 0 dropped; step-6 guidance scan not repeated (refreshed 2026-06-30, 5 days prior)
2026-07-05  architecture-drift  done  branch chore/maintenance-docs-2026-07-05  boundary heat map clean (arch-go green; cross-context imports only via api/testkit/bootstrap/migrations/coordination); fixed stale `architecture.md` data-plane narrative post-ADR-0015 (5->7 contexts, ingest now writes ClickHouse archive + MySQL event_queue in visibility, dropped MySQL `events` table from the persistence table, added `alert_event_payloads`); ADR-0004 intent unchanged
2026-07-05  config-surface-review  done  branch chore/maintenance-docs-2026-07-05  first run: 36 live knobs (23 server + 13 agent), 0 dead/wrong-layer/should-be-constant (allowlists already DB-backed #459, tuning already constants); fixed 2 stale config.go comments (EDR_CLICKHOUSE_DSN now required not "optional/MySQL-only"; EDR_RETENTION_DAYS prunes process records not events); flagged for user: dead `edr.retention.rows_deleted` metric, vestigial rules `ConfigKnob`/`env_var` API, stale operations.md retention narrative
2026-07-05  observability-review  done  no code changes  OTel-only policy clean (no prometheus/`/metrics`); route+metric coverage broad (event_queue/auth-revocation/ingest metrics present); 2 EDR SigNoz dashboards (RED + auth/authz) unchanged+healthy, other 3 are Fleet MDM's; 0 alert rules (MTTD/telemetry-loss still #348); GAP flagged: new ClickHouse-archive-write + control-channel-gRPC boundaries lack dedicated latency/error/conn metrics
2026-07-05  ai-review-bot-config-audit  done  no findings  `.coderabbit.yaml`: all 8 path_instruction globs match the tree (identity matrix 6/6 present, breakglass, 2 rego, agent/extension, scripts, test, catch-all); multi-platform held (Windows `agent/wintel` covered by `{agent,extension}/**`, no rewrite); new visibility/observability contexts covered by the Go catch-all; Qodo `.pr_agent.toml` keys valid (auto agentic_review on open+push); no deprecated keys, config unchanged since 06-18
2026-07-05  claude-config-audit  done  branch chore/maintenance-docs-2026-07-05  committed fix: stale "five bounded contexts"->seven in tracked `.claude/skills/find-prior-art/SKILL.md` (was misinforming prior-art searches); corrected the task-file's wrong "`.claude/` is gitignored / none committed" scope note (commands + skills ARE tracked; only settings.local.json + scheduled_tasks.lock are not); ntfy idle hook valid; noted local settings.local.json allow list grew to 178 with a kept `Read(~/work/fleet/**)` path
2026-07-05  consolidation-pass  done  branch chore/maintenance-code-2026-07-05  repo dup 0.7% (674 lines/27 blocks/17 files), aggregate cog 6751 over 65737 ncloc; deduped `rules/internal/appcontrol/store.go` (#2 cog file at 211) row-scan loops into `scanHostGroups`/`scanRuleRow`/`scanRules` mirroring the existing `scanPolicyRow` (2 dup groups / 4 blocks / 56 lines -> 0, net -12 lines); build + vet + AppControl integration green. Deferred to issues: signedtoken<->satoken cross-context dup, operator/handler.go boilerplate, ProcessTree.tsx cog-253
2026-07-05  dead-code-sweep  done  no findings  Go: `staticcheck -checks=U1000` (upgraded to 2026.1/v0.7.0 for Go 1.26; the old v0.4.7 segfaulted) reports 0 unused first-party symbols across server/agent/internal (also CI-gated by golangci `unused`+`staticcheck`). TS: `ts-prune` 0 genuine dead exports (all 12 hits are "used in module" false positives). Config knobs already covered clean by config-surface-review. Routes not re-swept (issue-filing territory, unchanged since 05-19)
2026-07-05  test-suite-health  done  no code changes  14 Go skips all legit env/mode/platform guards (MySQL/ClickHouse DSN, git, macOS/Windows, -short, PBT known-state, PATH), 0 TS/Swift skips, no stale perma-skips; `-race` single pass green (2367 tests, 0 fails, 0 data races); slowest ~14s (catalog ESF-fixture replays) + ~10-11s real-MySQL integration (SSO/trace-sampler/appconfig), essential not accidental (no sleeps; DSN-gated so unit runs stay fast); appcontrol list tests substantively cover the consolidation refactor, no tautology smells. Bounded to count=1 this cycle (last count=3 clean 2026-06-18, minimal churn since); no new thinness issues to file
2026-07-05  consolidation-pass  done  branch chore/maintenance-consolidation-2026-07-05  baseline dup 0.6% (23 blocks / 618 lines), aggregate cog 6706 over 65712 ncloc (down from 0.7%/6751 pre-#618). Two clusters, both behavior byte-identical (existing PBT/integration suites assert each; [no-behavior-change]/[no-docs-change]). (1) Shared HTTP request/response primitives into `httpserver`: `ReadCappedBody`/`DecodeCappedJSON` (the limit+1 read + `len>limit` check, the exact invariant CodeRabbit flagged on #618) replace 5 hand-rolled copies across two contexts (operator appcontrol/detectionconfig + identity useradmin/saadmin/ssoadmin); `WriteJSONError` centralizes the identity `{"error":code}` envelope behind the 3 identical `writeErr` wrappers. (2) Cross-context token dup: the `v1.<payload>.<mac>` HMAC-SHA256 envelope (split/mac/Seal + the strict-decode + constant-time verify core) was mirrored in `endpoint/internal/signedtoken` and `identity/internal/satoken`; extracted to new `internal/hmactoken` (shared via internal/ per ADR-0004, not sideways), each package keeping its own Claims + post-verify checks (kid/audience/expiry) and mapping the shared sentinels back to its own. hmactoken 97.5% cov, signedtoken 92.6%, satoken 87.5%. Deferred to next passes: `ProcessTree.tsx` cog-253 (top hotspot, single-session-plus + just touched in #619, file as issue per task rule), operator two-field error-writer shape
2026-07-05  consolidation-pass  done  branch chore/consolidation-processtree-operator-2026-07-05  cleared the two candidates deferred from the prior entry, both behavior byte-identical ([no-docs-change]). (1) `ProcessTree.tsx` was the top cognitive-complexity file (~253) but carried NO S3776 gate violation (the metric is spread across many sub-threshold functions; only OPEN issue was a MINOR S7735 negated-condition). Relocated the D3 render logic (`renderTree` + 11 pure per-node helpers + the render-only constants + `RenderResult`/`TreeInteractions`) into a new sibling `ProcessTree.render.ts` (910->647 lines; render.ts 271 lines, 99% stmt / 100% line cov via the existing component suite, 62 tests unchanged), lowering the .tsx file metric by moving the concern out; fixed the S7735 nit in the moved `chevronGlyph`. (2) operator two-field error envelope centralized in `writeOperatorErr` (handler.go); `writeAppControlErr`/`writeDetectionConfigErr` now delegate (all 56 call sites unchanged). Next hotspots unchanged.

2026-08-20  todo-fixme-sweep  done  no findings  4th consecutive clean run. Only markers in tracked first-party source are upstream's in vendored `server/apidocs/embed/redoc.standalone.js`; `.build/` Swift hits are untracked; `go test -run XXX` in `server/testdb/full/bench_test.go` is the idiomatic match-nothing flag, not a marker
2026-08-20  memory-and-claudemd-audit  done  branch `chore/maintenance-2026-08-20`  CLAUDE.md said Codecov is "held non-gating at 70%", which is true only of the PROJECT rollup: the PATCH gate is `if_ci_failed: error` and blocked two PRs this session (#725 at 65.2%, #730 at 21.4%). Rewrote to name both gates and to say patch coverage must be measured with `-tags=integration` or the integration-only paths read 0% locally. Contexts (7) and every `task` target and path reference verified clean. MEMORY.md (uncommitted): marked `esf_timestamps`, `control_channel_split_brain`, `procdetail_network_correlation` as FIXED with the PR numbers and carried forward the fork-to-exec-gap anti-lesson
2026-08-20  test-suite-health  done  no code changes  full `-race` pass with both DSNs exported: 115 packages, 3474 tests, 0 failures, 0 data races, and 0 skips. The 14 skips reported on 2026-07-05 were all DSN/platform gates and vanish when provisioned, which is worth recording: an unprovisioned run silently under-tests by that much
2026-08-20  dead-code-sweep  done  no findings  Go `staticcheck -checks=U1000` (2026.1) clean across server/agent/internal; `ts-prune` 0 genuine dead exports (16 of 17 hits are "used in module"). Trap re-confirmed from 07-05: the `staticcheck` on PATH is still v0.4.7 and SEGFAULTS on Go 1.26, so it must be run via `go run honnef.co/go/tools/cmd/staticcheck@2026.1` or the sweep silently reports nothing
2026-08-20  ai-review-bot-config-audit  done  no findings  `.coderabbit.yaml`: all 8 path_instruction globs match the tree, all 5 disabled-tool CI refs resolve to real workflows, no deprecated keys; `.pr_agent.toml` keys valid (agentic_review on open + push). New `visibility`/`observability` contexts stay covered by the Go catch-all
2026-08-20  adr-audit  done  no findings  18 ADRs, every status line agrees with its README index row, 0 Proposed remaining, no decision reversed by this cycle's work (the agent-side `InFlight` set is agent state, not the server state ADR-0010 governs; the control-stream heartbeat rides the connection registry ADR-0010's amendment already covers). Carried forward from 07-05 and still unfiled: recommended ADRs for TLS-1.3-only-with-no-1.2-opt-out and for no-in-product-auto-update
2026-08-20  config-surface-review  done  no findings  all three items flagged for the user on 2026-07-05 are resolved: the dead `edr.retention.rows_deleted` metric is gone (docs now cite the live `edr.retention.processes.rows_deleted`), the vestigial rules `ConfigKnob` API is gone, and the operations.md retention narrative is correct. The four tunables added this cycle (`PendingCommandTTL`, `lastSeenWriteTimeout`, `defaultFloorInterval`, `defaultSilenceDeadline`) are all constants, matching the established "tuning is a constant, not a knob" pattern
2026-08-20  claude-config-audit  done  no findings  10 tracked files under `.claude/` (4 opsx commands, 6 skills): no stale context counts, every referenced path and `task` target resolves. Untracked `settings.local.json` unchanged at 178 allow entries / 0 deny since 07-05, so nothing to prune
2026-08-20  observability-review  done  branch `chore/maintenance-2026-08-20`  route coverage holds: the whole mux is wrapped by otelhttp in `httpserver.Build`, so this cycle's new `POST /api/commands/{id}/cancel` inherits route-templated `http.server.request.duration` with no per-route work. GAP found and fixed in-comment, flagged for the user: `response.Service.CountPending` has NO production caller (cmd/main's `serverGaugeSource` adapts only endpoint `CountActive` + detection `CountOfflineHosts`, and the processor-backlog gauge is the visibility context's own different `CountPending`), yet `bootstrap.go` claimed the metrics adapter consumed it. Being on `api.Service` is why `unused` cannot see it. 07-05's control-channel/ClickHouse metric gap is unchanged
2026-08-20  best-practices-refresh  done  branch `chore/maintenance-2026-08-20`  2 items re-evaluated against code, both stale in the same direction (shipped but still unchecked): Application Control `[ ]`->`[~]` (named policies, host-group scoping and all six matching dimensions ship with a keyed index each in the extension's store; Detect-vs-Protect is the only part outstanding, the `Enforcement` column accepts DETECT but every rule runs PROTECT), and DNS query monitoring `[ ]`->`[x]` (the NE's `DNSProxyProvider` is the host resolver, `dns_query` events reach the ClickHouse archive, the process-detail panel and the `dns_c2_beacon` rule). Also fixed the stale `server/rules/api/types.go` claim that only BINARY is enforced, which the extension's six keyed indexes contradict
2026-08-20  consolidation-pass  done  branch `chore/maintenance-2026-08-20`  Sonar's two highest-density duplication entries were the same file twice: `BlockNotification.swift` at 69.6% in the host app and 47.4% in the extension, byte-identical apart from one extension-only constant and divergent comments. Both copies carried a comment warning that renaming any string is a silent wire-shape break, which is a warning where a mechanism belongs. Moved to `shared/BlockNotificationContract.swift` (one definition, folder-synchronised into every target that needs it, the same arrangement `XPCEventServer.swift` already uses) and added `shared` to the host-app target's `fileSystemSynchronizedGroups`, the one pbxproj line it needed. Package.swift's `sources`/`exclude` entries repointed. Verified: xcodebuild all targets, `swift test` 233 tests, SwiftLint clean. Review round added the part I had underestimated: deleting the two copies orphaned FIVE comments that still described the two-copy design, in `Package.swift`'s exclude rationale, the `com.fleetdm.edr.notify.plist` MachServices note, and the test file's header plus its wire-bytes comment. Copilot caught three of them and I caught two. Worth internalising as a rule of thumb for the next dedup: a duplicate that survived because a comment was policing it leaves roughly one stale comment per copy per reader, and the plist one had been quietly wrong even before this change (it named `NotificationClient.swift` as reading `blockNotificationServiceName`, which it never has; the only production reader is a log line in `main.swift`). The same round found `CountPending`'s "used by the OTel metrics gauge" claim repeated in `response/api/service.go` and `response/internal/mysql/store.go`, which would have left the tree contradicting the bootstrap correction in this same PR
2026-08-20  threat-model-and-security-refresh  done  branch `chore/maintenance-2026-08-20`  3 deltas, all from this cycle's response-context work: a new authn/authz row for suppression of a response action (withdrawing a command requires the authority to ISSUE that command type, not read access, because preventing a response is itself a response decision; refused once acked, and a late ack reopens a withdrawn command so the record reports what ran); component 1's kill row now records that a queued command is bounded in time, since `kill_process` addresses a reusable pid; component 8's repudiation and DoS rows now cover the control-stream heartbeat, its ordering ahead of the bounded last-seen write, and its drop-rather-than-queue behaviour on a full buffer. 0 threats demoted, no new gaps
2026-08-20  architecture-drift  done  no findings  `task lint:arch` green (that is the invocation CI uses; there is no `task arch-go` target, and asking for one exits non-zero in a way that reads like a failed check). Independent import map across all seven contexts: the only cross-context imports are `server/testdb/full` (test helper) and detection -> `server/coordination/leader`, both sanctioned; everything else goes through `api/`. ADR-0004 intent holds, and this cycle's new shared code went to `internal/hmactoken`-style shared locations rather than sideways between contexts
2026-08-20  doc-accuracy-sweep  done  branch `chore/maintenance-2026-08-20`  scanned 580 committed markdown files for broken paths, unresolvable doc-to-doc links and unknown `task` targets. 1 genuine finding: ADR-0007 pointed at `task package:agent`, which has never existed; the packaging target is `pkg:release`, so the sentence now names the pipeline rather than a fictional command. Everything else the scan surfaced was a false positive of the same two shapes, worth writing down so the next run does not re-chase them: relative shorthand after a sentence already names the directory (`policy/data/roles.json`, `fixtures/auth.ts`, `api/api_test.go`), and historical prose in `log.md` and `openspec/changes/archive/**` that correctly records paths as they were at the time. External URLs not checked this run
2026-08-20  stale-implementation-references  done  branch `chore/maintenance-2026-08-20`  cleared the one item 2026-07-05 deferred: `openspec/specs/server-availability/spec.md` cited `ai/migrations/recommendation.md` and `ai/migrations/ha-architecture.md`, both gitignored, so the Purpose paragraph's rationale resolved to nothing for every reader. Now cites ADR-0009 (already linked) and ADR-0011. Mechanism note for the next run, because this is why it was deferred twice: a Purpose-only correction CANNOT be expressed as an OpenSpec change (validate rejects a change with no `## ADDED/MODIFIED/REMOVED Requirements` section), and no archived modification delta has ever carried a `## Purpose`, only new-capability deltas do, so `openspec archive` does not own that prose and hand-editing it is both safe and the only path. Remaining `ai/` mentions in committed files are all deliberate (ignore-file globs and SECURITY.md's out-of-scope list). No shipped-but-described-as-pending or dead-branch references found
2026-08-20  architecture-anti-patterns  done  no findings  0 package-level mutable containers across `server/` (ADR-0010 holds, unchanged since the first run on 06-30); wrong-layer SQL scan clean once HTTP `DELETE /route` registrations are excluded, which is the refinement 06-30 added and an ad-hoc grep re-learns the hard way; no speculative abstraction in this cycle's new seams (`internal/hmactoken` has two real consumers, `commander.InFlight` has two transports, and the consumer-side one-implementation interfaces are the repo's Go convention rather than a finding). Step-6 guidance scan not repeated (refreshed 06-30)
